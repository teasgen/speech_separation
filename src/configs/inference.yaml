defaults:
  - model: dprnn
  - metrics: ss
  - datasets: ss_dataset_val
  - dataloader: example
  - transforms: ss_full_spec
  - _self_
inferencer:
  device_tensors: ["mix_spectrogram", "s1_spectrogram", "s2_spectrogram", "mix", "s1", "s2", "s2_video", "s1_embedding", "s1_embedding", "s2_embedding"] # which tensors should be on device (ex. GPU)
  device: auto # device name or "auto"
  save_path: "inferenced" # any name here, can be a dataset name
  seed: 42
  from_pretrained: "/home/teasgen/dla/speech_separation/saved/dprnn_v1_kernel_7_retest_max_norm_10_lr_1e3/model_best.pth"
